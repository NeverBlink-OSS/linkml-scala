package millbuild

/** The shared library's naming, shared between the build and the packaging scripts. */
object NativeLib {

  /** The library's base name. The platform's extension is added on top. */
  val name = "liblinkml_scala"
}
