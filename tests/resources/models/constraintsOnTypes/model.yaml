id: https://neverblink.eu/linkml/tests/constraintsOnTypes/
name: constraintsOnTypes
imports:
  - linkml:types

# TODO LNK-41: symlink instances to be the same as `constraints`

classes:
  Typed:
    tree_root: true
    slots:
      - int_slot
      - float_slot
      - string_slot

slots:
  int_slot:
    rank: 1
    range: small_int
  float_slot:
    rank: 2
    range: small_float
  string_slot:
    rank: 3
    range: patterned_string

types:
  small_int:
    base: int
    minimum_value: -1
    maximum_value: 1
  small_float:
    base: float
    minimum_value: -2.0
    maximum_value: 2.0
  patterned_string:
    base: str
    pattern: "^([0-9]{3})?[0-9]{3}-[0-9]{4}$"
