id: https://neverblink.eu/linkml/tests/uri/
name: uri
prefixes:
  ex: http://example.org/

emit_prefixes:
  - ex

imports:
  - linkml:types

classes:
  SomeOtherClass:
    attributes:
      id:
        identifier: true
        range: uri
  SomeClass:
    tree_root: true
    attributes:
      some_slot:
        range: uri
      some_other_slot:
        range: SomeOtherClass
