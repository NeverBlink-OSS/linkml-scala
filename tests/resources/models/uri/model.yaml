id: https://neverblink.eu/linkml/tests/uri/
name: uri
prefixes:
  ex: http://example.org/

emit_prefixes:
  - ex

imports:
  - linkml:types

classes:
  SomeOtherClass:
    attributes:
      id:
        identifier: true
        range: uri
  SomeClass:
    tree_root: true
    attributes:
      some_slot:
        rank: 1
        range: uri
      some_other_slot:
        rank: 2
        range: SomeOtherClass
